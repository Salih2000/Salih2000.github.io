<br>
<hr>
<br>

{{ site.data[site.active_lang].strings.citation.introduction }}

{% capture citation_quote -%}
> {{ site.data['en-us'].strings.name.last }}, {{ site.data['en-us'].strings.name.first }}{% if site.data['en-us'].strings.name.middle %} {{ site.data['en-us'].strings.name.middle }}{% endif %} ({{ page.date | date: "%b %Y" }}). {{ page.title }}. {% if site.title != 'blank' %}{{ site.title }}. {% endif %}{{ site.url }}.
{%- endcapture %}
{{ citation_quote | markdownify }}

<p>{{ site.data[site.active_lang].strings.citation.bibtex }}</p>

{% capture citation_code -%}
```bibtex
@article{ {{- site.data['en-us'].strings.name.last | downcase }}{{ page.date | date: "%Y" }}{{ page.title | slugify }},
  title   = { {{- page.title -}} },
  author  = { {{- site.data['en-us'].strings.name.last }}, {{ site.data['en-us'].strings.name.first }}{% if site.data['en-us'].strings.name.middle %} {{ site.data['en-us'].strings.name.middle }}{% endif -%}},
  {%- if site.title != 'blank' %}journal = { {{- site.title -}} },{% endif %}
  year    = { {{- page.date | date: "%Y" -}} },
  month   = { {{- page.date | date: "%b" -}} },
  url     = { {{- site.url }}{{ page.url -}} }
}
```
{%- endcapture %}
{{ citation_code | markdownify }}
